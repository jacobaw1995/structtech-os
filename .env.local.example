# Supabase project: structtech (the shared multi-tenant project — see docs/SCOPE.md §7/§9)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Server-only. Never expose to the client, never used in Client Components.
# Needed later for admin/seed scripts — not required to boot the Week 1 shell.
SUPABASE_SERVICE_ROLE_KEY=
